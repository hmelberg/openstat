#options.view = dashboard
#options.mode = brython
#options.title = "Iris-dashboard (Brython)"
#options.description = "Raskt dashboard uten Pyodide — laster på et par sekunder"
# load https://raw.githubusercontent.com/hmelberg/openstat/main/data/iris.csv as iris
import pandas_brython as pd
import plotly_express_brython as pe

#input art = dropdown("setosa", "versicolor", "virginica")
#input min_lengde = slider(4, 8, step=1, default=4, label="Min. sepal-lengde")

#%% Fordeling, wide
sub = iris[iris["species"] == art]
sub = sub[sub["sepal_length"] >= min_lengde]
pe.histogram(sub, x="sepal_length", title=art)

#%% Antall blomster, row=kpi
print(len(sub))

#%% Snitt sepal-lengde, row=kpi
print(round(sub["sepal_length"].mean(), 2))

#%% Snitt sepal-bredde, row=kpi
print(round(sub["sepal_width"].mean(), 2))

#%% Sepal mot petal, wide
pe.scatter(sub, x="sepal_length", y="petal_length",
           title="Sepal- mot petal-lengde")
