#r
# Variable-level assembly in R mode: the datasets are built by the shared
# executor and loaded into R as data.frames before the analysis runs.

# connect https://raw.githubusercontent.com/mwaskom/seaborn-data/master/penguins.csv as pen
# load pen as penguins

aggregate(body_mass_g ~ island, data = penguins, FUN = function(x) mean(x, na.rm = TRUE))

# ── Import + join (sources with a common key) ──
# connect https://x.example/personer.csv as p
# create-dataset panel, key(pid)
# import p/income, p/region into panel
# aggregate(income ~ region, data = panel, FUN = mean)
