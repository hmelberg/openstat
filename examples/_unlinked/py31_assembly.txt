#py
# Variable-level assembly (Project A): build one dataset from several sources.
# connect names the source, load/import/join reference the connect alias.

# connect https://raw.githubusercontent.com/mwaskom/seaborn-data/master/penguins.csv as pen

# create-dataset study, key(rowid)
# The sources here have no common key, so the example below uses penguins
# directly as a whole table:
# load pen as penguins

penguins.groupby("island")["body_mass_g"].mean()

# ── Pattern with import + join (requires sources with a common key column) ──
# connect https://x.example/personer.csv as p      # columns: pid, income, region
# connect https://x.example/salg.csv as s          # columns: pid, amount
# create-dataset panel, key(pid)
# import p/income, p/region into panel
# load s as sales
# join sales into panel on pid                       # left-join (default); inner/outer optional
# panel.groupby("region")["income"].mean()
