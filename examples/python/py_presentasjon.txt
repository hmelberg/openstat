# label: Notebook — presentation (slides)
#options.mode = python
#options.title = "Presentation — slides from cells"
#options.description = "the slide= attribute groups cells into slides; arrow keys/click zones navigate, Esc exits; #options.view = present opens shared links straight into the presentation"
# load https://raw.githubusercontent.com/hmelberg/openstat/main/data/iris.csv as iris

#%% md slide=1
# Iris as a presentation

This document is also a presentation: `slide=1` on the cell marker
(or `#tag.slide = 1` at the top of the cell body) groups cells into
slides. Cells without a number follow the previous cell.

Pick **Presentation** in the view menu at the bottom left. Arrow keys or
the click zones at the edges switch slides; Esc exits. Add
`#options.view = present` to the preamble so a shared link opens
straight into the presentation.

#%% python slide=2 hide-code
iris.groupby("species")["sepal_length"].mean()

#%% md
Mean sepal length per species — the code cell above has `hide-code`, so
only the result is shown on the slide. This text cell has no slide
number of its own and follows the previous cell (slide 2).

#%% python slide=3
#tag.hide-code = true
import plotly.express as pe
pe.scatter(iris, x="sepal_length", y="sepal_width", color="species")
