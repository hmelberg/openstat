# label: #@param forms (Colab-compatible)
#options.mode = python
#options.title = "#@param forms (Colab-compatible)"
#options.description = "Colab-style comment forms: #@param builds a control above the cell and writes the value straight into the source text"
# load https://raw.githubusercontent.com/hmelberg/openstat/main/data/iris.csv as iris

#%% md
# #@param forms

Notebooks from Google Colab that use the `#@param` comment work
**directly** here — no conversion needed. A line like

```python
n = 5  #@param {type:"slider", min:0, max:20}
```

gets a control drawn above the cell; change the control, and the new
value is written straight into **the source text itself** (check
«Raw text» — it's always the text, not hidden state, that is the
source of truth).

**`run:"auto"` vs. manual:** auto is the default — the cell reruns
automatically every time a control changes. Add `{run:"manual"}` (on a
param line, or on a `#@title` line to set the whole cell's default) to
only write the value; the form then shows a Run chip until you run the
cell yourself.

`#@title` and `#@markdown` (Colab's cell-title/text comments) also work:
a `#@title` line renders as a heading above the form, and its
`{run:"manual"}` meta (if any) becomes the cell's run default; each
`#@markdown` line renders as prose in the form strip, in source order.

Add `placement:"left"` (or `"top"`/`"bottom"`) to the metadata object
to place that particular control in a different strip than the cell's
default.

**R is also supported:** `x <- 3  #@param {type:"slider", min:0, max:10}`
— the parser recognizes both `=` and R's `<-` as assignment operators.

#%% python
#@title Row count
#@markdown Choose how many rows of the iris dataset to preview.
n = 5  #@param {type:"slider", min:0, max:20, step:1}
iris.head(n)

#%% python
species = 'setosa'  #@param ["setosa", "versicolor", "virginica"]
iris[iris["species"] == species]

#%% python
all_columns = True  #@param {type:"boolean"}
iris if all_columns else iris[["sepal_length", "petal_length", "species"]]

#%% python
color = 'blue'  #@param ["blue", "red", "green"] {allow-input:true}
f"Favorite color: {color}"

#%% python
date = '2026-07-15'  #@param {type:"date"}
f"Selected date: {date}"
