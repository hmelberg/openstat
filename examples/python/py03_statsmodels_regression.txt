# label: statsmodels — OLS regression
# Example: statsmodels — OLS regression with a formula
# `# load` fetches the data. statsmodels.formula.api runs OLS.
# load https://raw.githubusercontent.com/hmelberg/openstat/main/data/person_year_sample.csv as reg

import statsmodels.formula.api as smf

reg["income_thousand"] = reg["income"] / 1000
model = smf.ols("income_thousand ~ age + C(status)", data=reg).fit()
model.summary()
