# label: pandas + #micro
# Example: pandas on a microdata dataset
# The first block imports variables via microdata.
# The second block uses pandas on the result (the dataset name becomes the variable name).

#micro
require no.ssb.fdb:54 as fd
create-dataset folk
import fd/BEFOLKNING_KJOENN as kjonn
import fd/INNTEKT_WLONN 2022-01-01 as inntekt

#python
folk.head()

folk.groupby("kjonn")["inntekt"].agg(["mean", "median", "count"])
