# label: Notebook — cells with #%% (python + markdown)
#options.mode = python
#options.title = "Notebook cells"
#options.description = "Cells are separated by #%% — markdown, code, and attrs like hide-code"
# load https://raw.githubusercontent.com/hmelberg/openstat/main/data/iris.csv as iris

#%% md
# Iris notebook
This document is a **notebook**: cells are separated by `#%%`.
Columns view places output next to each cell; Stacked puts it below;
Output only shows just the results. The «Raw text» button gives you the
plain editor.

#%% python
iris.groupby("species")["sepal_length"].mean()

#%% python hide-code
iris.describe()

#%% md style=note
The cell above has `hide-code` — only the result is shown.

#%% md
The ▶ button on each code cell runs only that cell; Shift+Enter runs and
moves on, Ctrl/Cmd+Enter runs in place. «Restart & run all» resets the
session and reruns everything from the top.
