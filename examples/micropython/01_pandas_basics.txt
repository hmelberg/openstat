# label: pandas_mpy — basics
# MicroPython mode: fast Python in the browser
import pandas_mpy as pd

df = pd.DataFrame({
    "city":  ["Oslo", "Bergen", "Oslo", "Bergen", "Tromsø"],
    "year":  [2023, 2023, 2024, 2024, 2024],
    "value": [10, 20, 30, 25, 15],
})
show(df)
df.groupby("city")["value"].mean()
